part of 'index.dart';

class MPHeight extends SizedBox {
  MPHeight(double height, {super.key}) : super(height: height.h);
}

class MPWidth extends SizedBox {
  MPWidth(double width, {super.key}) : super(width: width.w);
}
